// Secrets.xcconfig.example — committed template. Copy to Secrets.xcconfig and fill in your keys.
// Secrets.xcconfig is gitignored and must never be committed.

AVIATION_STACK_KEY = YOUR_AVIATIONSTACK_KEY_HERE
AVIATION_STACK_SCHEME = http
// Switch AVIATION_STACK_SCHEME to https when upgrading to AviationStack Standard plan ($29.99/mo).
